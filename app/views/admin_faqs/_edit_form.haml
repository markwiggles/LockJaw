
%h3 Edit FAQ

.container.form-md

  =simple_form_for @admin_faq, :url => {:action => 'update'}, remote: true do |f|

    =f.input :id, :as => :hidden

    -# The switch button
    =render partial: 'admin_shared/switch_form', locals: {f: f, model_attribute: @admin_faq, :param => 'admin_faq'}

    =f.input :question, autofocus: true, class: 'faq'
    =f.input :answer, as: :text, :required => true, class: 'faq',:input_html => {:rows => 10, :cols => 54}
    %br
    =f.button :submit, 'Update FAQ', class: 'btn-default'



:javascript

  var ready = function () {


    /*Call the custom jQuery method to initialise the switches
     jQuery.fn.initialiseSwitches = function (urlPost, urlCallback) */
  $('.switch').initialiseSwitches(null,null);

  };

  $(document).ready(ready);
  $(document).on('page:load', ready);

